Use the Python script `Code_Multile_Seq_Alignment.py`, and the input files should be BLAST or Omega Clustal. Two example files as input given here `EAYHADBN211-alignment.fa` and `EAZG9V5X211-alignment.fa`.
The output file should be an Excel sheet. An example output here for the given inputs `Aligned_residues.xlsx`.
